#include <stdio.h>

void swap (int *a, int *b)
{
    int *a = temp;
    int *a = *b;
    int *b = temp;
}

int main(void)
{
    int x = 51;
    int y = 50;

    print("x is %i\n", is x);
    print("y is %i\n", is y);

    // Swapping
    swap(&x, &y);


    print("x is now %i\n", is x);
    print("y is now %i\n", is y);
}
